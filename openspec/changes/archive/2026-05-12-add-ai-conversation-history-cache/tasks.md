## 1. 规格与现状确认
- [x] 1.1 确认 `createAIConversation()` 事务关闭旧 active 会话的行为与当前 bug 触发路径。
- [x] 1.2 确认入口页、详情页、header 新建按钮的调用关系。

## 2. 测试先行
- [x] 2.1 添加服务层或 API 测试：创建新会话时旧 active 会话被关闭。
- [x] 2.2 添加并发测试：同一客户并发创建后只保留一个 active 会话。
- [x] 2.3 添加权限测试：未登录、跨用户历史列表和跨用户消息访问被拒绝。
- [x] 2.4 添加入口页行为测试：进入 `/customer/conversations` 不再先请求会话列表。
- [x] 2.5 添加缓存状态测试：历史列表可从缓存读取并刷新。
- [x] 2.6 添加消息缓存测试：旧会话消息按 userId + conversationId 缓存。
- [x] 2.7 添加缓存隔离测试：退出登录或切换账号后不展示上一个账号缓存。
- [x] 2.8 添加缓存刷新测试：新建、关闭、删除、评分或新增消息后相关缓存更新或失效。
- [x] 2.9 添加 UI 状态测试：历史面板 loading、empty、error、retry 状态可见且可操作。
- [x] 2.10 添加分页测试：历史列表首屏 20 条，消息首屏最近 50 条，并可加载更多。
- [x] 2.11 添加资源测试：缓存 TTL、列表上限、会话上限、每会话消息上限和淘汰行为。
- [x] 2.12 添加首屏请求测试：新对话入口可输入前不请求历史列表或历史消息。
- [x] 2.13 添加缓存字节上限测试：序列化缓存超过 2 MB 时淘汰最久未使用缓存。
- [x] 2.14 添加 quota 错误测试：缓存写入失败时清理缓存且对话功能保持可用。
- [x] 2.15 添加 `?new=1` 一次性测试：仅刚创建会话跳过首屏消息加载，旧会话仍加载消息。
- [x] 2.16 添加历史列表请求去重测试：同一 userId + history-list + page/cursor 不重复并发请求。
- [x] 2.17 添加消息请求去重测试：同一 userId + conversationId + page/cursor 不重复并发请求。

## 3. 实施
- [x] 3.1 简化 `/customer/conversations`：直接创建新会话并跳转。
- [x] 3.2 修复旧 active 会话关闭链路，确保以认证用户 ID 和服务端事务为准。
- [x] 3.3 在 AI 对话页增加历史入口，并按需加载旧会话列表。
- [x] 3.4 处理并发创建，保证最终只有一个 active 会话。
- [x] 3.5 实现历史面板 UI 状态：loading、empty、error、retry，移动端使用抽屉或全屏面板。
- [x] 3.6 扩展 conversation store，按 userId 缓存历史列表，按 userId + conversationId 缓存消息。
- [x] 3.7 实现缓存容量、TTL 和淘汰策略。
- [x] 3.8 实现历史列表分页和旧会话消息分页。
- [x] 3.9 实现缓存序列化字节上限和 quota 错误处理。
- [x] 3.10 打开旧会话时加载对应消息；`?new=1` 仅对刚创建会话一次性跳过历史消息请求。
- [x] 3.11 使用后移除或忽略 `?new=1` 标记，失配时按普通历史会话加载消息。
- [x] 3.12 对历史消息和加载更早消息按 userId + conversationId + page/cursor 去重。
- [x] 3.13 在会话和消息 mutation 后更新或失效相关缓存。
- [x] 3.14 对历史列表和加载更多按 userId + history-list + page/cursor 去重。
- [x] 3.15 避免历史面板重复打开造成重复并发请求。
- [x] 3.16 保持现有 Flash/Pro 模式、评分、复制和流式回复行为不变。

## 4. 验证
- [x] 4.1 运行新增测试并确认先失败后通过。
- [x] 4.2 运行相关 API / store / page 测试。
- [x] 4.3 运行 `npm run type-check`。
- [x] 4.4 对本次改动文件运行 eslint。
- [x] 4.5 验证新对话入口首屏请求数量：不包含历史列表和历史消息请求。
- [x] 4.6 验证客户端缓存不会超过规格上限。
